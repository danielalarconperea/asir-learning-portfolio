---
title: "Human-in-the-Loop (HITL) Architecture Implementation"
author: "Antigravity Agent"
date: "2026-05-06"
tags: ["hitl", "soc", "security", "mqtt", "ai", "gemini", "zero-trust"]
---

# Human-in-the-Loop (HITL) Security Architecture

## Overview
This document outlines the transition of the Sentinel-IT SOC Coordinator (Raspberry Pi 5) from an autonomous blind-execution model to a Zero-Trust, Privilege-Escalation Conditioned model (HITL). This change mitigates the risk of AI prompt injection and hallucinated destructive commands.

## Core Components

### Triage Agent (AI Core)
The `SOC_Triage_Agent` is the central intelligence unit (Google Gemini ADK). It analyzes incoming raw logs and telemetry from the edge nodes.
- **Role:** Level 1 SOC Analyst.
- **Context Injection:** Uses `recommendations.json` as a dynamic knowledge base for mitigation strategies.

### IoT Tools (Guardrails)
The execution capabilities of the agent are strictly sandboxed using specific tool definitions in `src/tools/iot_tools.py`.
- **`execute_diagnostic_command`**: A read-only sandbox. Allows the AI to run whitelist commands (`sudo iptables -L`, `cat`, `ls`) for forensic investigation.
- **`request_mitigation_approval`**: A quarantine mechanism. Instead of executing destructive commands (e.g., `kill -9`, `iptables -A`), the AI proposes the command and its rationale, which is stored in the SQLite database with a `PENDING` status. The tool also accepts an optional `revert_command` for the exact rollback when the agent can know it safely.

### Dashboard SOC (Control Plane)
The web interface (`src/dashboard_soc.py` and `src/templates/index.html`) acts as the human intervention point.
- **Endpoint:** `/api/mitigate/approve` handles the HTTP POST requests from the operator.
- **Interactive Modal:** A glassmorphism-styled UI component where the administrator can review the AI's `rationale`, edit the proposed `pending_command` via an interactive `<textarea>`, and finalize the execution (`APPROVED`) or discard it (`REJECTED`).
- **Revert Modal:** The operator can edit the command that will be sent as rollback. The modal prefers the stored `revert_command`; otherwise it only derives conservative inverses. If no safe inverse exists, it leaves the textarea empty for manual input.

### Database (State Management)
The SQLite `logs` table (`src/database.py`) manages the mitigation lifecycle.
- **New Columns:**
  - `status`: Tracks the state (`LOGGED`, `PENDING`, `APPROVED`, `REJECTED`).
  - `pending_command`: The raw Bash string proposed by the AI.
  - `revert_command`: Optional rollback command proposed by the AI or derived by PI-5 when safe.
  - `rationale`: The forensic reasoning generated by the AI for proposing the mitigation.

## Workflow Execution
1. The **PI-4 Edge Sensor** sends a malicious log via MQTT to AWS IoT Core.
2. The **PI-5 Triage Agent** ingests the log.
3. The Agent optionally uses `execute_diagnostic_command` to gather more context.
4. The Agent determines a threat and uses `request_mitigation_approval` to propose a firewall block.
5. The command enters the database as `PENDING`.
6. The human operator opens the Dashboard, clicks "Revisar Mitigación" on the live feed.
7. The operator edits the command if necessary and clicks "Ejecutar Comando".
8. The command is published via AWS IoT Core MQTT back to the PI-4 for native execution.

## Synchronous Publish on Approval (PUBACK gating)

The dashboard cannot afford fire-and-forget semantics on the approval step. If the MQTT publish were merely queued in the awscrt event loop, a silent disconnection or an AWS IoT policy rejection would leave the operator with a green "Mitigación aplicada correctamente" toast while the command never reaches PI-4.

To prevent that, `dashboard_soc.py` calls `mqtt_client.publish(topic, payload, wait_for_ack=True)` in both `approve_mitigation` and `revert_action`. The helper blocks until QoS 1 PUBACK is received from AWS IoT (5 s timeout) and raises on failure. The endpoint then:

- On success: writes `status='APPROVED'` (or `'REVERTED'`) and returns `200`.
- On failure: returns `502 Bad Gateway` with the publish error, **without** touching the database. The row stays `PENDING` so the operator can retry once connectivity is restored.

The agent-driven publishes inside `iot_tools.py` are limited to diagnostic reads classified as `SAFE_READ`; they keep the default fire-and-forget mode to avoid blocking the ADK runner thread. Any command classified as `LOW`, `HIGH`, or `CRITICAL` waits for a human decision in HITL. See [`funcionamiento_mqtt.md`](funcionamiento_mqtt.md#modos-de-publicación-asíncrono-vs-síncrono-puback) for the full comparison.

### Zombie connection detection

Before each HITL publish, `get_mqtt_client()` calls `AWSMqttClient.is_alive()` to detect "zombie" connections — where the Python object exists but the underlying TCP/TLS socket is dead. If detected, the zombie is disconnected and a fresh connection is established with exponential backoff. This prevents the 502 errors that occur when the dashboard's MQTT connection dies silently after a transient network failure. See [MQTT_Resilience.md](MQTT_Resilience.md) for the full architecture.

## End-to-end Execution Feedback

PUBACK from AWS proves the broker accepted the message, **not** that PI-4 executed it. The HITL flow closes that operator-facing loop using the sensor's own `seguridad/<device>/respuesta` channel and the `estado_mitigacion` field.

### Flow on approval

1. Dashboard POST `/api/mitigate/approve` (or `/revert/<id>`).
2. Backend re-classifies the edited command, signs the payload, publishes with `wait_for_ack=True`, and UPDATEs the row to `status='APPROVED'`, `estado_mitigacion=NULL`.
3. Response: `200 {status: 'dispatching', log_id}` (not `'success'`).
4. Frontend opens a 30-second poll loop on `/api/mitigate/status/<log_id>`, showing a blue "Esperando confirmación de PI-4..." toast.

### Flow on PI-4 response

5. PI-4 executes the command and publishes the result.
6. `main_coordinator.process_event` receives `seguridad/+/respuesta`, normalizes the payload to the canonical five-field shape, and enqueues it for the feedback agent.
7. The feedback agent calls `update_alert_status`, which writes `EXITO`, `FALLO`, or `RECHAZADO_FIRMA` into the latest row for that device.

### Flow on the dashboard

9. Within 1 s the frontend poll sees `phase='executed'` (or `'failed'`) and shows a green toast: *"PI-4 confirmó la ejecución del comando."* The row refreshes with the result attached.
10. If 30 s pass without a populated `estado_mitigacion`, the frontend gives up with a red toast: *"Sin respuesta de PI-4 tras 30s. Revisa la conectividad del sensor."* The row stays `APPROVED` with empty mitigation state so the operator can retry.

### Anomaly path (zero-trust)

If PI-4 receives an unsigned, expired, replayed, or forged command, it refuses execution before spawning a shell and emits `status="rejected_signature"` on the response topic. The feedback agent records that rejection; it does not reissue commands to the same sensor.

### Why there is no dispatch cache now

The old `record_dispatch` / `match_feedback` cache was reactive: it detected anomalies after a response came back. Ed25519 signing moved that guarantee to PI-4 before execution, which is the stronger boundary for this system.
