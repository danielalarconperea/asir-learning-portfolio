[pytest]
testpaths = tests
; tests/manual/ contiene scripts E2E que requieren AWS IoT real (mTLS),
; Gemini y/o el coordinador corriendo. Se ejecutan a mano, nunca con pytest:
; varios conectan a AWS en codigo de nivel de modulo.
addopts = --ignore=tests/manual
